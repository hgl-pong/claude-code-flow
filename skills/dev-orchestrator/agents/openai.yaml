interface:
  display_name: "Claude Code Flow Agents"
  short_description: "Use the shared oracle, forge, prism, and sentinel agent roster from the repository root."
  default_prompt: "Use $dev-orchestrator with the shared root agents: oracle for planning, forge for implementation, prism for acceptance, sentinel for review."

shared_agents:
  source: "../../../agents"
  roster:
    - oracle
    - forge
    - prism
    - sentinel
