---
name: ulw
description: "Legacy alias for ULI autonomous execution. Use /uli or type uli/ulw/ultrawork; this command forwards users to the ULI autonomous loop."
argument-hint: "<goal>"
allowed-tools:
  - Read
---

# ULW Merged Into ULI

`ulw` is now a legacy alias for ULI. Use `/uli <goal>` or type `uli`, `ulw`, or `ultrawork` in the prompt.

Completion uses the ULI completion tag: `<uli-done>`.
